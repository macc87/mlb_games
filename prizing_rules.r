x <- c(1:3500)
y <- c(10000, 
       5000, 
       2500, 
       1500, 
       1000, 
       750, 
       500, 
       400, 
       c(gl(1,2))*300,
       c(gl(1,2))*250,
       c(gl(1,3))*200,
       c(gl(1,5))*150,
       c(gl(1,10))*100,
       c(gl(1,20))*75,
       c(gl(1,30))*50,
       c(gl(1,50))*40,
       c(gl(1,70))*30,
       c(gl(1,100))*20,
       c(gl(1,200))*15,
       c(gl(1,300))*12,
       c(gl(1,400))*10,
       c(gl(1,700))*8,
       c(gl(1,1600))*6)
par(mfrow=c(1,1))
plot(x,y)

f <- splinefun(x,y)

par(mfrow=c(2,1))
plot(x,y)
plot(x,f(x))